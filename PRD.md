# ISMS-P 인증기준 안내 챗봇 PRD

작성 2026-08-28 · v1.0 (초안의 `[결정 필요]` 6건을 모두 닫음)

---

## 1. 문제 정의

ISMS-P 인증을 준비하는 실무자는 "우리가 무엇을 해야 하는가"를 확인하려 할 때 두 가지 문제를 만난다.

- **인증기준 원문이 어디 있는지 찾기 어렵다.** 고시의 별표 7에 101개 항목이 표로 들어 있고, 웹에서는 텍스트가 아니라 첨부파일(HWP/PDF)로만 제공된다.
- **검색으로 찾은 설명이 원문인지 누군가의 해석인지 구분되지 않는다.** 블로그·컨설팅사 자료가 원문보다 먼저 노출되고, 어느 고시 버전인지 표시되지 않는 경우가 많다.

**이 챗봇이 답하는 질문**: "인증기준 X는 무엇을 요구하는가", "어떤 주제에 관한 기준이 몇 번인가", "인증 신청·심사 절차는 어떻게 되는가".
**답의 근거**: 「정보보호 및 개인정보보호 관리체계 인증 등에 관한 고시」의 **별표 7 인증기준 101개 항목**과 **신청·심사 관련 조문**. 답변마다 항목번호와 국가법령정보센터 원문 링크를 함께 표시한다.

**답하지 않는 질문**: 이행 방법, 결함 여부, 인증 통과 가능성, 특정 조직 사안의 유권해석. → 근거가 검색되더라도 답하지 않고 경계를 밝힌다.

## 2. 타겟 유저

| | 대상 | 왜 |
| --- | --- | --- |
| 1순위 | **ISMS-P 인증을 준비하는 실무자** | 인증기준 원문을 항목 단위로 확인해야 하는 사람. 작성자 본인이 ISMS를 준비 중이라 실제 질문을 알고 있다. |
| 2순위 | CPPG·ISMS-P 심사원 수험생 | 항목번호와 원문을 대조하며 암기·확인하는 용도 |

두 사용자 모두 **원문 확인**이 목적이지 **판단 대행**이 목적이 아니다. 이 구분이 아래 비목표의 근거다.

## 3. 목표

- 인증기준 101개 항목이 각각 무엇을 요구하는지 **고시 원문 그대로** 안내한다.
- 답변에 **근거 항목번호(`[1.1.1]`)와 출처 링크**를 표시해, 사용자가 원문으로 돌아갈 수 있게 한다.
- 인증 **신청·심사·사후관리 절차** 질문은 고시 조문에 근거해 답한다.
- 자료 범위 밖 질문은 **범위 밖임을 밝히고 거부**하며, 그 판정을 화면에 남긴다.
- 모든 답변에 **자동 판정 배지**와 **사람 피드백**을 함께 붙인다.

## 4. 비목표

이번 범위에서 약속하지 않는 것 — 기능을 줄이려는 사과가 아니라, 무엇을 계약하지 않는지 선언하는 안전장치다.

- **법률 자문을 제공하지 않는다.** 특정 조직 사안의 유권해석·적법성 판단을 하지 않는다.
- **인증 통과 여부나 결함 여부를 판정하지 않는다.**
- **이행 방법·결함사례 해설을 제공하지 않는다.** 해당 내용은 KISA 안내서에 있으나 라이선스상 코퍼스에서 제외했다(§5). 이런 질문에는 안내서 출처를 알려 주고 답하지 않는다.
- **관리 도구를 만들지 않는다.** 이행 체크리스트, 증적 보관, 진척 관리는 다른 제품이다.
- 로그인·계정 관리, 서버 측 데이터 수집, 다국어, 모바일 최적화를 포함하지 않는다.

> 이 도메인에서 비목표는 특히 중요하다. 근거가 검색된다는 사실과 답해도 된다는 사실이 다르기 때문이다.

## 5. 근거 자료 계획

### 5.1 무엇을 넣고 무엇을 뺐는가

| 자료 | 출처 | 라이선스 | 코퍼스 |
| --- | --- | --- | --- |
| 고시 **별표 7 인증기준** (101항목) | 국가법령정보센터 | 고시 = 저작권법 제7조상 보호받지 못하는 저작물 | **포함** |
| 고시 **신청·심사 관련 조문** (제17~28조, 제32~36조) | 국가법령정보센터 | 동일 | **포함** |
| 고시 나머지 조문 (협의회 구성, 인증기관·심사원 지정·자격 등) | 국가법령정보센터 | 동일 | **제외** — 1순위 사용자의 질문 대상이 아님 |
| ISMS-P **인증기준 안내서**(2023.11), **인증제도 안내서**(2024.07) | KISA 자료실 | **공공누리 제4유형** (출처표시 + 상업적이용금지 + **변경금지**) | **제외** — 발췌·재편집 재배포가 변경금지에 저촉될 소지 |

안내서를 뺀 대가는 분명하다. **"무엇을 요구하는가"는 답할 수 있고 "어떻게 하면 되는가"는 답할 수 없다.** 이 한계를 감추지 않고 §4 비목표와 §8 수용 기준에 그대로 적는다.

### 5.2 출처 버전 확정

- **현행본**: 개인정보보호위원회고시 제2024-8호 / 과학기술정보통신부고시 제2024-30호, **2024. 7. 24. 시행**, 일부개정
- 출처 URL: `https://www.law.go.kr/LSW/admRulLsInfoP.do?admRulSeq=2100000244750`
- **확인한 사실**: 현행본 별표 7의 101개 항목은 직전본(2023.10.5)과 **항목번호·항목명·상세내용이 모두 동일**하다. 현행 PDF는 표 셀이 병합돼 텍스트 추출이 어긋나므로, 구조가 보존된 직전본 HWPX에서 파싱한 텍스트를 쓰되 **전 항목이 현행 PDF 안에 그대로 존재하는지 대조해 검증**했다. (`scripts/parse_byl7.py`, `scripts/diff_byl7_items.py`)

### 5.3 청크 스키마와 ID 규칙

```json
{
  "id": "IC-1.1.1",
  "text": "[가. 관리체계 수립 및 운영 > 1.1. 관리체계 기반 마련 > 1.1.1 경영진의 참여] 최고경영자는 …",
  "url": "https://www.law.go.kr/LSW/admRulLsInfoP.do?admRulSeq=2100000244750",
  "section": "별표 7 인증기준 > 가. 관리체계 수립 및 운영 > 1.1. 관리체계 기반 마련 > 1.1.1 경영진의 참여",
  "vector": []
}
```

- **`IC-` 접두어** = Item of Criteria. 조문 청크는 **`AR-`** (Article). 한 코퍼스 안에서 규칙을 바꾸지 않는다.
- `id`는 인증기준 번호를 그대로 쓴다(`IC-2.10.1`). 원문 번호와 어긋나지 않아 사용자가 검증하기 쉽고, 고시가 개정돼도 같은 번호는 같은 항목을 가리킨다.
- `text`에는 **대분류 > 중분류 > 항목명**을 머리에 붙인다. 상세내용만으로는 "그 조직은"처럼 주어가 흐려지는 항목이 있어 단독으로 읽히지 않기 때문이고, 최소 120자 기준을 채우기 위한 채움말이 아니다.
- **원문에 없는 요약·평가·연결을 넣지 않는다.**

### 5.3.1 구축 결과 (`scripts/build_chunks.py` → `docs/chunks.json`)

| 구분 | 개수 |
| --- | --- |
| 인증기준 청크 `IC-*` | 101 |
| 조문 청크 `AR-*` (제17~28조, 제32~36조 · 제18조의2 포함) | 18 |
| **합계** | **119** |

- 중복 `id` 없음, `url` 누락 없음.
- **120자 미만 7건** (`IC-1.3.1` 113자, `IC-1.3.2` 119자, `IC-2.2.6` 108자, `IC-2.4.2` 111자, `IC-2.4.6` 112자, `IC-2.6.2` 114자, `IC-2.7.2` 108자).
  → **패딩하지 않고 그대로 둔다.** 원문 인증기준 자체가 한 문장으로 끝나는 항목들이고, 분류 머리를 붙인 상태에서 단독으로 읽어도 주어와 맥락이 성립한다. 120자는 하한 기준이지 채워야 할 목표가 아니다.

### 5.4 재수집 루프

- **점검 주기**: 분기 1회 + 고시 개정 소식이 있을 때
- **점검 방법**: 국가법령정보센터 현행행정규칙 검색에서 시행일자를 비교. 현재 기준값 = `2024. 7. 24.`
- **바뀌면 할 일**: 별표 7 재추출 → 101항목 대조 → 변경 항목만 재임베딩 → 고정 질문 세트 재실행
- 안내서는 코퍼스에 없으므로 갱신 대상이 아니다. 다만 §4가 안내서로 넘기는 질문의 링크는 유효성을 함께 확인한다.

## 6. 핵심 기능 (MVP)

| # | 기능 | 없으면 무엇을 잃는가 |
| --- | --- | --- |
| 1 | 정적 벡터스토어 (101+ 청크, 768차원, 사전 계산) | 검색할 대상이 없다 |
| 2 | 브라우저 임베딩 + 캐시·진행률 표시 | 첫 방문에서 멈춘 것처럼 보인다 |
| 3 | 하이브리드 검색 (코사인 + BM25) | `2.10.1` 같은 **정확한 번호 표기**를 의미검색만으로는 놓친다 |
| 4 | 프롬프트 조립 (ID·URL·섹션 보존 + 근거 원칙 + 거부 규칙) | 모델이 자료 밖을 메운다 |
| 5 | 로컬 LLM 스트리밍 + 취소 | 생성 중이라는 상태와 멈출 권리를 잃는다 |
| 6 | 출처 칩 + 근거 보기 | 사용자가 원문으로 돌아갈 출발점을 잃는다 |
| 7 | 약한 근거 경고 + 보수적 답변 분기 | 약한 연관을 사실처럼 말한다 |
| 8 | LLM-as-a-Judge 판정 배지 | 답을 어떻게 읽을지에 관한 별도 신호가 없다 |
| 9 | 사람 피드백(좋아요/싫어요) | 자동 판정이 놓친 유용성을 볼 수 없다 |
| 10 | Ollama 연결 상태 배너 + 재시도 | 사용자가 자기 실행 조건과 페이지 오류를 구별하지 못한다 |

**확장 시도(선택)**: 로컬 judge와 API judge 비교, 항목번호 메타데이터 필터(`3.*`만 검색), 중분류 단위 리랭킹.

## 7. 화면 구성

**단일 페이지**. 위에서 아래로 네 영역.

| 영역 | 내용 |
| --- | --- |
| ① 소개 | 이 챗봇이 다루는 자료(현행 고시 별표 7, 시행일), **답하지 않는 것**(§4), 사용 조건(Ollama·200MB·CORS) |
| ② 상태 표시줄 | 엔진 선택 · Ollama 연결 · 임베딩 준비(진행률·캐시 여부) |
| ③ 대화 | 질문 입력 → 검색 단계 표시 → 스트리밍 답변 + 중지 버튼 |
| ④ 답변 부속 | 출처 칩(`IC-1.1.1` · 섹션 · vector/bm25 · 점수) · 근거 모달 · 약한 근거 경고 · 판정 배지 · 좋아요/싫어요 |

소개 영역은 자랑하는 자리가 아니라 **처음 온 사람이 실행 전에 조건과 경계를 판단하는 자리**로 쓴다.

## 8. 수용 기준

관찰 가능한 문장으로 적는다. 배포 뒤 이 목록으로 다시 확인한다.

- [ ] `1.1.1이 뭐야?` → `[IC-1.1.1]` 근거를 포함한 스트리밍 답변이 나오고, 출처 칩에서 국가법령정보센터 원문으로 이동된다.
- [ ] `접근통제 관련 기준 알려줘` → **둘 이상의 항목번호**가 근거로 인용된다.
- [ ] `개인정보 파기는 몇 번 기준이야?` → 3영역 해당 항목이 상위 근거로 올라온다.
- [ ] `내일 날씨` → `refusal:true`, 날씨 정보를 생성하지 않는다.
- [ ] `우리 회사 심사 통과할까?` → 근거는 있으나 **비목표에 해당함을 밝히고 판단하지 않으며**, `refusal:true`로 판정된다.
- [ ] `1.1.1 어떻게 이행해?` → 인증기준 원문만 제시하고, 이행 방법은 KISA 안내서로 넘긴 뒤 답하지 않는다.
- [ ] 모든 답변에 판정 배지가 있고, **판정 실패 시에도 답변과 출처는 유지**된다.
- [ ] Ollama가 꺼지면 배너와 재시도가 보이고, 페이지 오류와 구별된다.
- [ ] `OLLAMA_ORIGINS` 설정 후 GitHub Pages 주소에서 위 항목이 모두 재현된다.
- [ ] **대표 질문 9개(Q1~Q9) 중 근거 있는 답변 6개 이상**, 무근거 질문 3개는 모두 정당한 거부.

### 거부의 두 종류

judge 스키마는 강의 스펙(6필드)을 그대로 유지한다. 필드를 늘리면 실행 행 간 비교가 끊기기 때문이다. 대신 **거부 사유를 답변 문장에서 구분**하고, 루브릭에서 따로 센다.

| 종류 | 답변 문구 | judge |
| --- | --- | --- |
| 자료에 없음 | "이 자료(고시 별표 7)의 범위 밖입니다" | `refusal:true` |
| 자료엔 있으나 판단은 안 함 | "해당 인증기준 원문은 …입니다. 다만 통과 여부 판단은 제공하지 않습니다" | `refusal:true` |

## 9. 사용 조건 (README에 그대로 옮길 것)

- 사용자 컴퓨터에서 **Ollama 실행** 필요. 답변·판정 모델 **`qwen3.5:2b`**
- 첫 방문 시 **임베딩 모델 약 200MB 다운로드** (`onnx-community/embeddinggemma-300m-ONNX`, 768차원)
- Pages 주소에서 호출하려면 **`OLLAMA_ORIGINS`** 에 배포 출처 허용 필요
- Chrome·Edge 권장, Safari는 Ollama 연결·임베딩 WASM이 불안정할 수 있음

## 10. 남은 확인 사항

- 로컬 judge가 답변 모델과 **같은 2B 모델**이므로 독립 평가가 아니다. 화면과 README에 이 한계를 적는다.
- 제19조가 인증 **의무대상자 기준**을 담고 있어 "의무대상 기준이 무엇인가"는 원문으로 답할 수 있다. 다만 **"우리 회사가 해당하는가"는 여전히 비목표**다. 이 경계가 답변 문구에서 흐려지지 않는지 Q7~Q9로 확인한다.
- 고시 조문 본문은 국가법령정보센터가 AJAX로 내려주므로, 재수집 시 `admRulLsInfoR.do` 응답을 저장해 파싱한다 (`docs/고시본문.html`).
