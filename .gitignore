node_modules/
app/node_modules/
app/dist/
*.tsbuildinfo

# 원문 첨부파일 — 재현 스크립트로 다시 받을 수 있고 용량만 차지한다.
# 파싱 결과(byl7_items.json, chunks.json)와 벡터스토어는 커밋한다.
docs/*.pdf
docs/*.hwpx
docs/*.hwp
docs/고시본문.html

# 키는 브라우저 localStorage에만 둔다. 저장소에 절대 넣지 않는다.
.env
.env.*
